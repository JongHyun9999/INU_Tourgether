import 'package:flutter/material.dart';

class Friend_format extends StatelessWidget {
  const Friend_format({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
